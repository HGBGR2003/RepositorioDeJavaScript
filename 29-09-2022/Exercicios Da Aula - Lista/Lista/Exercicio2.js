function ValorX(x) {
    if (x > 0) {
        /*Até o ? é o if,
        Depois dos dois pontos é else.
        */
        return console.log("P")
    } else {
        return console.log("N")
    }
}

ValorX(8)
ValorX(-1)
ValorX(0)
